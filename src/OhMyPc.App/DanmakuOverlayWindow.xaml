<Window x:Class="OhMyPc.App.DanmakuOverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        ShowActivated="False"
        ShowInTaskbar="False"
        Topmost="True"
        Focusable="False"
        IsHitTestVisible="False">
    <Canvas x:Name="Stage" ClipToBounds="True">
        <Border x:Name="MessagePanel"
                Background="{StaticResource OverlaySurfaceBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="4"
                Padding="16,11"
                MaxWidth="720">
            <Border.RenderTransform>
                <TranslateTransform x:Name="Motion" />
            </Border.RenderTransform>
            <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition Width="4" /><ColumnDefinition Width="14" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                <Border x:Name="SeverityBar" CornerRadius="2" />
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock x:Name="TitleText" FontWeight="Bold" Margin="0,0,10,0" VerticalAlignment="Center" />
                    <TextBlock x:Name="BodyText" Foreground="{StaticResource TextBrush}" VerticalAlignment="Center" TextWrapping="Wrap" />
                </StackPanel>
            </Grid>
        </Border>
    </Canvas>
</Window>
